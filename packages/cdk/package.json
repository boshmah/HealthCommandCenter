{
  "name": "@health-command-center/cdk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "deploy:domain": "cdk deploy DomainStack --require-approval never",
    "deploy:auth": "cdk deploy AuthStack --require-approval never",
    "deploy:api": "cdk deploy ApiStack --require-approval never",
    "deploy:frontend": "cdk deploy FrontendStack --require-approval never",
    "deploy:cloudfront": "cdk deploy CloudFrontStack --require-approval never",
    "deploy:all": "pnpm deploy:domain && pnpm deploy:auth && pnpm deploy:api && pnpm deploy:frontend && pnpm deploy:cloudfront",
    "synth": "cdk synth",
    "diff": "cdk diff"
  },
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "@types/node": "20.11.5",
    "aws-cdk": "2.118.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "~5.3.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.118.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21",
    "@health-command-center/types": "workspace:*"
  }
}
